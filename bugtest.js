function Sum(val) {
    return val.val1 + val.val2
}

console.log(Sum(random = {
    val1: 2,
    val2: 1
}))



this.template.setMetaTags(sampleJsObject = {
    title: this.title,
    description: this.config.item('default-keywords'),
    keywords: this.config.item('default-keywords')
});